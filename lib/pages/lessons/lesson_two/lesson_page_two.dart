import 'package:flutter/material.dart';

class LessonPageTwo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lesson Page 2'),
      ),
      body: Center(
        child: Text('This is Lesson Page 2'),
      ),
    );
  }
}