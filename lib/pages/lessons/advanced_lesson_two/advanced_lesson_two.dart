import 'package:flutter/material.dart';

class AdvancedLessonTwo extends StatelessWidget {
  const AdvancedLessonTwo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Advanced Lesson 2'),
      ),
      body: const Center(
        child: Text('This is Advanced Lesson 2'),
      ),
    );
  }
}