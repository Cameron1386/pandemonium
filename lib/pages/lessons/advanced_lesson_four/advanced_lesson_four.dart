import 'package:flutter/material.dart';

class AdvancedLessonFour extends StatelessWidget {
  const AdvancedLessonFour({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Advanced Lesson 1'),
      ),
      body: const Center(
        child: Text('This is Advanced Lesson 1'),
      ),
    );
  }
}