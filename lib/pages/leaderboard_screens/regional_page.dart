import 'package:flutter/material.dart';

class RegionalPage extends StatelessWidget {
  const RegionalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Regional Leaderboard'),
      ),
    );
  }
}